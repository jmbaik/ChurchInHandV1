import React from 'react';
import Icon from 'react-native-vector-icons/Foundation';

export default function FoundationIcons({name, size, color}) {
  return <Icon name={name} size={size} color={color} />;
}
