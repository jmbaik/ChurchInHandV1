import React from 'react';
import Icon from 'react-native-vector-icons/FontAwesome';

export default function FontAwesomeIcons({name, size, color}) {
  return <Icon name={name} size={size} color={color} />;
}
