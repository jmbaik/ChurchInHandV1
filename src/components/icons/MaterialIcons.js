import React from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';

export default function MaterialIcons({name, size, color}) {
  return <Icon name={name} size={size} color={color} />;
}
